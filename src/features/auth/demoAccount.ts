/**
 * Reviewer account created in the Backendless `Users` table so lecturers can try the
 * protected Create Blog flow without registering. Change the password in the console
 * (Data > Users) if the app is ever used for anything real.
 */
export const demoAccount = {
  email: 'demo@example.com',
  password: 'Suitmedia123!',
}
